# Secrets — never commit
.env

# Python
__pycache__/
*.py[cod]
.venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# OS / editor noise
Thumbs.db
.DS_Store

# Unity (for when the client package lands; harmless now)
[Ll]ibrary/
[Tt]emp/
[Ll]ogs/
UserSettings/
*.csproj
*.sln
# ...but the hand-written client projects are source, not Unity-generated:
!client/**/*.csproj

# Claude Code personal overrides
CLAUDE.local.md

#Ignoring research folder, since it's just a big, unecessary upload.
Research Papers/
