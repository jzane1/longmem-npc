[pytest]
testpaths = tests
addopts = -p no:cacheprovider
markers =
    nlp: calls the write pass at the service level (triggers the lazy spaCy+fastcoref load - minutes, once per process). The Stop hook runs the subset -m "not nlp" (ruled 2026-07-20); the full suite runs on demand and at floor verification.
