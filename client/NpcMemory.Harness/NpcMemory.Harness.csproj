<Project Sdk="Microsoft.NET.Sdk">

  <!-- The dotnet-run console harness (unity-client.md, ruled 2026-07-27):
       plays every demo beat headless against a live `python -m app.serve`
       — the Wk-1 Unity<->backend interop go/no-go, proven before Unity
       ever opens. -->

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net8.0</TargetFramework>
    <RootNamespace>NpcMemory.Harness</RootNamespace>
    <Nullable>enable</Nullable>
    <ImplicitUsings>disable</ImplicitUsings>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\NpcMemory.Core\NpcMemory.Core.csproj" />
  </ItemGroup>

</Project>
